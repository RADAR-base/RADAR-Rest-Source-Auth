version: '2'
services:
    postgresql:
        image: postgres:9.6.2
        ports:
          - "5432:5432"
        environment:
          - POSTGRES_USER=radarcns
          - POSTGRES_PASSWORD=radarcns
          - POSTGRES_DB=deviceauthorizer
